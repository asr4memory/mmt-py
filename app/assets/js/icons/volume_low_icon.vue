<template>
    <!-- Filled style: solid speaker cone with stroked marks. The single arc is
         the inner one of volume_on_icon, which draws the same arc plus a wider
         second one, so that the two icons read as one series. -->
    <svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
        <path d="M4 9 H7 L11.5 5 V19 L7 15 H4 Z" fill="currentColor" />
        <path
            d="M15 9.2 a4 4 0 0 1 0 5.6"
            fill="none"
            stroke="currentColor"
            stroke-width="1.8"
            stroke-linecap="round"
            stroke-linejoin="round"
        />
    </svg>
</template>
