name: App Tests

on:
  push:
    branches:
      - master
    paths:
      - "app/**"
      - ".github/workflows/app-tests.yml"
  pull_request:
    paths:
      - "app/**"
      - ".github/workflows/app-tests.yml"
  workflow_dispatch:

jobs:
  backend:
    runs-on: ubuntu-latest
    env:
      DJANGO_ENV: test
      VITE_DEV_MODE: false
      SECRET_KEY: test-secret-key
      DATABASE_NAME: mmt
      DATABASE_USER: root
      DATABASE_PASSWORD: root
      DATABASE_URL: mysql://root:root@127.0.0.1/mmt
      EMAIL_URL: dummymail://
      EMAIL_FROM: bob@example.com
      CELERY_BROKER_URL: redis://localhost

    services:
      mysql:
        image: mysql:8
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: mmt
        ports:
          - 3306:3306
        # The job waits for the container to report healthy before the first
        # step runs, so the interval is short: with a 10s interval most of the
        # wait is spent between two checks rather than on the server starting.
        options: >-
          --health-cmd="mysqladmin ping"
          --health-start-period=5s
          --health-interval=2s
          --health-timeout=5s
          --health-retries=20

    steps:
      - uses: actions/checkout@v6
      - name: Set up Python 3.14
        uses: actions/setup-python@v6
        with:
          python-version: "3.14"
      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: app/package-lock.json
      - name: Install the lastest version of uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b
        with:
          enable-cache: true
      - name: Install system dependencies
        run: |
          sudo apt-get update --quiet
          sudo apt-get install \
            --assume-yes \
            --no-install-recommends \
            --quiet \
            ffmpeg \
            gettext \
            libharfbuzz-subset0 \
            libpango-1.0-0 \
            libpangoft2-1.0-0
      - name: Create upload temp directories
        working-directory: ./app
        run: |
          mkdir --parents user_files/_tmp
          mkdir --parents user_files_test/_tmp
      - name: Build assets with Node.js
        working-directory: ./app
        run: |
          npm ci
          npm run build
      - name: Prepare Django installation
        working-directory: ./app
        run: |
          uv sync --group dev
          uv run manage.py compilemessages --verbosity 0
      - name: Run Django tests
        working-directory: ./app
        run: |
          echo "Django $(uv run django-admin --version)"
          uv run pytest --ignore=mmt/core/tests/test_selenium.py --ignore=mmt/projects/tests/test_selenium.py

  frontend:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: app/package-lock.json
      - name: Install Node.js dependencies
        working-directory: ./app
        run: npm ci
      - name: Run the TypeScript typecheck
        working-directory: ./app
        run: npm run typecheck
      - name: Run Vitest tests
        working-directory: ./app
        run: npm test
